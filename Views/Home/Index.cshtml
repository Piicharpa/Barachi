@model BarachiModel.Models.Barachi

@{
    ViewData["Title"] = "Delete Barachi";
}

<!-- Include SweetAlert2 library via CDN -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<div class="text-center mb-5">
    <h1 class="display-4">Delete Barachi</h1>
</div>

<div class="container">
    <!-- 1. Added an id="deleteForm" to target it with JavaScript -->
    <form id="deleteForm" method="post" action="/Home/Delete">
        
        <!-- ROW 1: Barachi ID and PIC -->
        <div class="row mb-3">
            <div class="col-md-6 form-group">
                @Html.LabelFor(m => m.BarachiID, new { @class = "form-label fw-bold" })
                @Html.TextBoxFor(m => m.BarachiID, new { @class = "form-control", placeholder = "Enter Barachi ID..." })
                @Html.ValidationMessageFor(m => m.BarachiID, "", new { @class = "text-danger small" })
            </div>
            
            <div class="col-md-6 form-group">
                @Html.LabelFor(m => m.PIC, new { @class = "form-label fw-bold" })
                @Html.TextBoxFor(m => m.PIC, new { @class = "form-control", placeholder = "Enter PIC..." })
                @Html.ValidationMessageFor(m => m.PIC, "", new { @class = "text-danger small" })
            </div>
        </div>

        <!-- ROW 2: Reasons -->
        <div class="row mb-4">
            <div class="col-md-12 form-group">
                @Html.LabelFor(m => m.Reasons, new { @class = "form-label fw-bold" })
                @Html.TextAreaFor(m => m.Reasons, new { @class = "form-control", rows = 3, placeholder = "Enter reasons..." })
                @Html.ValidationMessageFor(m => m.Reasons, "", new { @class = "text-danger small" })
            </div>
        </div>

        <!-- SUBMIT ACTIONS -->
        <div class="row">
            <div class="col-12 text-end">
                <button type="submit" class="btn btn-danger px-4">Confirm Delete</button>
            </div>
        </div>
    </form>
</div>


<script>
    document.getElementById('deleteForm').addEventListener('submit', function (e) {
        // Prevent immediate submission to show the alert first
        e.preventDefault();

        // Dynamically fetch values entered by the user
        //  generates id matching the property name
        const barachiId = document.getElementById('BarachiID').value.trim();
        const pic = document.getElementById('PIC').value.trim();
        const reasons = document.getElementById('Reasons').value.trim();

        // Basic client-side check to ensure fields aren't completely blank
        if (!barachiId || !pic || !reasons) {
            Swal.fire({
                icon: 'error',
                title: 'Missing Information',
                text: 'Please fill out all fields before attempting to delete.'
            });
            return;
        }

        // Display the data inside SweetAlert html configuration
        Swal.fire({
            title: 'Are you absolutely sure?',
            html: `
                <p class="text-muted">Review the details you entered below:</p>
                <div style="text-align: left; background: #f8f9fa; padding: 15px; border-radius: 6px; border: 1px solid #dee2e6;">
                    <strong>Barachi ID:</strong> ${escapeHtml(barachiId)}<br>
                    <strong>PIC:</strong> ${escapeHtml(pic)}<br>
                    <strong>Reasons:</strong> <span style="white-space: pre-wrap;">${escapeHtml(reasons)}</span>
                </div>
                <br>
                <span class="text-danger fw-bold">This action cannot be undone!</span>
            `,
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#dc3545', // Bootstrap red danger color
            cancelButtonColor: '#6c757d',  // Bootstrap gray color
            confirmButtonText: 'Yes, delete it!',
            cancelButtonText: 'Cancel'
        }).then((result) => {
            // If the user clicks "Yes, delete it!", programmatically submit the form
            if (result.isConfirmed) {
                document.getElementById('deleteForm').submit();
            }
        });
    });

    // Helper function to prevent XSS vulnerability if input contains HTML special tags
    function escapeHtml(text) {
        return text
            .replace(/&/g, "&amp;")
            .replace(/</g, "&lt;")
            .replace(/>/g, "&gt;")
            .replace(/"/g, "&quot;")
            .replace(/'/g, "&#039;");
    }
</script>
